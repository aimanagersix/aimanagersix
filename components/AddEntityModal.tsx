import React from 'react';

/**
 * This is a placeholder component to resolve a potential build issue
 * caused by a file with a similar name (`AddEntidadeModal.tsx`).
 * This component is not used in the application.
 */
const AddEntityModalPlaceholder: React.FC = () => {
    return null;
};

export default AddEntityModalPlaceholder;
